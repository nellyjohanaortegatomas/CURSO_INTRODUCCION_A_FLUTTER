"Se configura Flutter en el sistema, se descarga desde el navegador oficial, se revisan los requisitos del sistema operativo, se recomienda instalar Git for Windows, se descarga el archivo zip y se coloca en la carpeta del Flutter SDK, luego se configura la variable de entorno para reconocer Flutter en el sistema, se instalan herramientas como Android Studio o Visual Studio Code con extensiones de Flutter y Dart, finalmente se ejecuta flutter doctor en CMD para verificar que todo este correctamente instalado" 
